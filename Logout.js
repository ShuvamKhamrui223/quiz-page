/*
Logic:
Create a variable isLogout, initialize with false and store into localStorage
onclick logout set isLogout true show a confirm popup
if(storage===user && isLogout)
show loginForm
else if(storage===user !isLogout)
show username
else if(storage!=user)
show create an account 
*/
